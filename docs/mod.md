# MOD

**Category:** Operator  
**Syntax:** `num1 MOD num2`

## Description

Computes the integer remainder (modulo) from the division of `num1` by `num2`.

## Example

```basic
10 LET A = 17 MOD 5
20 PRINT(A)
```
