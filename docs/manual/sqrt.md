# SQRT

**Category:** Function  
**Syntax:** `SQRT(num)`

## Description

Returns the square root of the specified `num`. The argument must be non-negative.

## Example

```basic
10 LET A = SQRT(16.0)
20 PRINT(A)
```
