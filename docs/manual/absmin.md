# ABSMIN

**Category:** Function  
**Syntax:** `ABSMIN(num1, num2)`

## Description

Returns the absolute value of the minimum of the two arguments.

## Example

```basic
10 LET A = ABSMIN(-10, 5)
20 PRINT(A)
30 LET B = ABSMIN(-20, -15)
40 PRINT(B)
```
