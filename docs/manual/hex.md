# HEX

**Category:** Print Format Modifier  
**Syntax:** `PRINT(HEX, expression_list)`

## Description

Used as a prefix inside a `PRINT` statement to format output representation of numbers in Hexadecimal format.

## Example

```basic
10 LET A = 255
20 PRINT(HEX, A)
```
