# COS

**Category:** Function  
**Syntax:** `COS(num)`

## Description

Returns the trigonometric cosine of the angle argument, which must be specified in radians.

## Example

```basic
10 LET A = COS(0)
20 PRINT(A)
```
