# BIN8

**Category:** Print Format Modifier  
**Syntax:** `PRINT(BIN8, expression_list)`

## Description

Used as a prefix inside a `PRINT` statement to format output representation in 8-bit space-grouped Binary format.

## Example

```basic
10 LET A = 255
20 PRINT(BIN8, A)
```
